engines:

  duplication:
    enabled: true
    config:
      languages:
        php:
          mass_threshold: 200
    exclude_fingerprints:

  fixme:
    enabled: true

  phpmd:
    enabled: true
    checks:
      CleanCode/StaticAccess:
        enabled: false
    exclude_fingerprints:

ratings:
  paths:
  - "**.php"
